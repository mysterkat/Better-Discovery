# ── FastAPI backend ─────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sse-starlette==2.1.3
httpx==0.27.2

# ── MONTE CARLO / Pattern Discovery toolkit ──────────────────────────────────
numpy==1.26.4
pandas==2.2.3
MetaTrader5
plotly>=5.20
scipy>=1.12
scikit-learn>=1.4
matplotlib>=3.8
beautifulsoup4>=4.12
lxml>=5.0
# v0.5.0: required for the long-term MC benchmark (e.g. ^GSPC). Used by
# mc_funded_test._fetch_benchmark to overlay a Buy & Hold reference line
# on the long-term equity fan. Without this dep the feature falls back to
# a noisy "yfinance not installed" warning at runtime.
yfinance>=0.2.40

# v1.0.0-dev: Optuna TPE optimizer (alternative to the GA in pattern
# discovery). Required when GENE_OPTIMIZER="optuna" in the Discovery tab.
# Without it, the optuna path raises a friendly error pointing here.
optuna>=4.0

# v1.3.0: LightGBM tree-based candidate generation (alternative to KMeans
# clustering). Required when CLUSTERING_METHOD="lightgbm" in the Discovery
# tab. Without it, the lightgbm path logs a warning and falls back to KMeans.
lightgbm>=4.0
