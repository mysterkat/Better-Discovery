/** Phase 5 placeholder — full UI implemented in Phase 6. */
export default function SetToMqlTab() {
  return (
    <div className="tab-placeholder">
      <h2>⚙ Set → MQL</h2>
      <p>
        Convert a Pattern Discovery <code>.set</code> file into a ready-to-compile
        MetaTrader 5 Expert Advisor using the bundled PatternDiscoveryEA template.
      </p>
      <span className="tab-coming-soon">Implementation in Phase 6</span>
    </div>
  );
}
